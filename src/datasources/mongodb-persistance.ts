import Persistence from './persistence';

export default class MongoDbPersistence implements Persistence {
  // ToDo
  public async getCollection(collection: string, filter: any): Promise<any | null> {
    //
    return null;
  }
}
